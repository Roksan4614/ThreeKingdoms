public enum StatusType
{
    Wait,

    Success,
    Failed,
    Cancel,

    Valid,
    Invalid,
}

public enum DirectionType
{
    NONE,

    Left, Right, Up, Down,

    MAX
}

public enum HeroSortType
{
    Grade, Level, Power,
}